import React from 'react';
import {Container} from './styles'

const Box = (props) => {
  return (
    <Container width={props.width} height={props.height}>
      
    </Container>
  )
}

export default Box;