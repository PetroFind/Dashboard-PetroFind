import styled from 'styled-components';

export const Container = styled.div`
  background-color: white;
  flex-direction: row;
  position: relative;
  overflow: hidden;
  position: relative;
  display: flex; 
` 